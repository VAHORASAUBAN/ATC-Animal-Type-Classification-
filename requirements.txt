flask==2.3.3
flask-cors==4.0.0
opencv-python==4.8.1.78
# Allow newer numpy wheels on newer Python versions (avoid source build on Py3.12)
numpy>=1.24.3,<2
# Allow newer pandas wheels for newer Python versions
pandas>=2.0.3,<3
# Allow newer scikit-learn wheels (avoid source compile that needs MSVC)
scikit-learn>=1.3.0,<2
joblib==1.3.2
Pillow==10.0.0
